extends ../layout

block content
  p Hi #{user.name},

  p
    | Client
    i #{client.name}
    | wants your permission to:

  ul
    each scope in scopes
      li=scope

  form(method="POST")
    input(type="hidden" name="_csrf" value=csrfToken)

    button(type="submit" name="decision" value="allow") Allow
    button(type="submit" name="decision" value="deny") Deny
